FROM python:3.12-slim

WORKDIR /app
ENV PYTHONDONTWRITEBYTECODE=1 PYTHONUNBUFFERED=1
COPY tensorlane /opt/tensorlane
RUN pip install --no-cache-dir "/opt/tensorlane[postgres]"
EXPOSE 8080
CMD ["tensorlane", "serve", "--host", "0.0.0.0", "--port", "8080"]
