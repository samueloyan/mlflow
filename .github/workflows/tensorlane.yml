name: Tensorlane

on:
  pull_request:
    paths:
      - "tensorlane/**"
      - "tests/tensorlane/**"
      - "deploy/compose/**"
      - "docs/adr/00[7-9]*"
      - ".github/workflows/tensorlane.yml"
  push:
    branches:
      - master
    paths:
      - "tensorlane/**"
      - "tests/tensorlane/**"
      - "deploy/compose/**"
      - ".github/workflows/tensorlane.yml"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: tensorlane-tests
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.10"
      - name: Install Tensorlane
        run: pip install -e "./tensorlane[dev]"
      - name: Run Tensorlane tests
        run: python -m pytest tests/tensorlane --confcutdir=tests/tensorlane -q
